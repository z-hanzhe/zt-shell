基于 Tauri2 + Rust 构建的跨平台桌面端 SSH 工具 ZTShell，全程由 AI 开发：

- 架构视野：开发及需求评估时应具备项目整体架构视野，主动识别并抽离可复用模块（样式/工具函数/组件等）
- 需求评估：收到需求后先评估技术合理性与实现复杂度，不合理或过高时主动提示并给出替代方案或分步建议
- 功能开发：优先审视现有架构；任务内可做消除结构冲突的必要重构。涉及数据迁移、外部行为变化或大范围模块调整时，先说明影响并取得确认
- 跨平台性：优先全平台兼容（Win/Mac/Linux/国产），至少保证 Win/Mac 良好兼容
- 第三方库：需求实现前判断自写或用库，用库需申请并附理由，注意跨平台兼容性

## 文档维护规范

- 文档定位：`.ai-assisted/markdown/` 是快速上手索引，不是代码副本；代码、配置和测试才是事实来源。任务开始先读 `index.md` 定位，仅加载需求涉及的模块文档；跨模块变更再扩展阅读
- 更新判断：仅在模块职责、入口、跨模块契约、持久约束或关键业务规则变化时更新。纯样式、局部实现和普通 Bug 修复不更新；不更新文档是正常且合格的结果
- 记录内容：只记录职责、入口、稳定约束/陷阱和关联模块。每条只表达一个稳定事实，优先合并或替换旧条目，禁止只追加
- 禁止内容：变量名、像素值、DOM/事件细节、算法过程、方案比较、历史变更，以及可从代码直接得出的实现说明
- 陷阱规则：仅记录会导致跨模块回归或反直觉行为的稳定约束，格式为“触发条件 -> 后果 -> 正确做法”，最多两句；不能借陷阱记录实现过程
- 维护范围：编辑文档时只复核当前模块及直接引用。全局陷阱只在 `index.md` 顶部的“⚠️ 全局陷阱速查表”维护，模块文档不得重复
- 格式约束：单一事实来源，其他位置只引用；文档与现状不一致时立即删除或修正，不保留历史说明
